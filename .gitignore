# ---------------------------------------------------------------------------
# NEVER COMMIT SECRETS OR RAW FINANCIAL DATA
# The password and session secret live in Render environment variables only.
# ---------------------------------------------------------------------------
.env
.env.*
*.local
secrets.json

# Raw financial source data — statements, exports, anything with names,
# addresses or card numbers. These belong on the home machine only.
raw/
derived/
*.pdf
*.CSV
statements/
statements-*/

# Node
node_modules/
npm-debug.log*
.npm/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
